# Monitoring Applications only — PrometheusRules/ServiceMonitors sync via kube-prometheus-stack app (Topic 11).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - loki/Application.yaml
  - promtail/Application.yaml
  - kube-prometheus-stack/Application.yaml
  - otel/Application.yaml
