# Kyverno controller Helm values — chart 3.2.7 / app 1.12.6 per versions.yaml
replicaCount: 1

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    memory: 512Mi

admissionController:
  replicas: 1

backgroundController:
  replicas: 1

cleanupController:
  enabled: true

reportsController:
  enabled: true

config:
  excludeKyvernoNamespace: true
